{
  "devDependencies": {
    "jest": "^28.1.3"
  },
  "scripts": {
    "test": "jest",
    "build": "parcel build index.html",
    "dev": "parcel index.html --open",
    "start": "npm run build && npm run dev"
  }

}
